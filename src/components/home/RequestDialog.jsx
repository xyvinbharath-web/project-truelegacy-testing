const RequestDialog = () => {
  return (
    <div>
      {/* Dialog placeholder; actual modal will be designed later */}
      Request Dialog (to be redesigned)
    </div>
  );
};

export default RequestDialog;

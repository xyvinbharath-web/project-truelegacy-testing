const SurveyForm = () => {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <h1 className="text-3xl font-semibold">Succession Survey (ready for new design)</h1>
    </div>
  );
};

export default SurveyForm;
